

template<class state_t ,class Op_t>
class eulerStepper
{
public:
  
  void step(state_t state2,state_t state1,real_t timeStep);
  {
    state2=op(state1)*timeStep;
    state2=stat1  + state2;
  }
private:
  Op_t op;
  
}
